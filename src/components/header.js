import React from "react";

export const Header = function (props) {

    // console.log(props.headerAndTitle)
    return (
        <>
            <h2>{props.headerAndTitle}</h2>
        </>
    );
};